using System.Collections;
using System.Collections.Generic;
using UnityEngine;


//This script holds and edits the hiker prefabs???
//Like it gets info from the hiker class or camper class, takes the ID that's put in,
//and then the hikerGenerator can call it to do shit. Like maybe itd be physicalHiker.spawnPhysical or something.
public class PhysicalHiker : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
