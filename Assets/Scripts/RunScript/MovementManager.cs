using UnityEngine;
using System.Collections;

//this script holds functions that are like: campers.moveforwardone 
//later this will be called automatically from game manager, will happen automatically as long as no OTHER commands are pressed, will run as many times
//as there are slots in the level.
public class MovementManager : MonoBehaviour
{

    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
